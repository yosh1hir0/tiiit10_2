# Bubble Sort Example in C++

Простой пример реализации алгоритма **сортировки пузырьком** на языке C++.

## 📌 Описание
Программа демонстрирует:
- Создание массива целых чисел.
- Вывод исходного массива.
- Сортировку массива методом пузырька.
- Вывод отсортированного массива.

## 🛠 Используемые технологии
- Язык: **C++**
- Компилятор: любой совместимый с стандартом **C++11** и выше

## 📂 Код программы
```cpp
#include <iostream>
using namespace std;

int main() {
    int arr[] = {64, 34, 34, 25, 61, 12, 22, 11, 90, 42, 99};
    int n = sizeof(arr)/sizeof(arr[0]);
    
    cout << "Original: ";
    for (int i = 0; i < n; i++) {
        cout << arr[i] << " ";
    }
    cout << endl;
    
    // Bubble sort
    for (int
